import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const activityImporterSelectors = importerSelectors(
  'activity.importer',
);

export default activityImporterSelectors;
